

export type projectData={
    userId:string,
    projectId:string,
    uuid:string,
    projectName:string,
    apiKey:string,
    clientId:string,
    apiSecret:string,
    createdAt:string,
    updatedAt:string,
}